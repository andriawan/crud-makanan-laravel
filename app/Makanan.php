<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Makanan extends Model
{
    protected $table = 'menu_makanan';
    // protected $fillable = ['nama_makanan, deskripsi, harga, created_at, updated_at'];
}
